# 0001 — Bootstrap decisions

- SpecDD wird als Inspiration genutzt, nicht als Basis.
- PHP-UI bleibt simpel: `php -S`, kein Framework, kein npm.
- Filenames für tickets/decisions/milestones/reports bleiben sequentiell numeriert (`NNNN-slug.md`).
